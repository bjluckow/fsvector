# v1.2 (Hotswapping Models) Notes

Milestones 1.2.4 and 1.2.5 ended up being implemented in previous milestones
    - model registry populated on startup with per-modality health checks; TEXT_EMBEDSVC_URL and IMAGE_EMBEDSVC_URL split in config and compose; DeactivateModels, ActiveModel, SetModelIndexedAt in store/registry.go